[package]
name = "sea-reference-math"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
[lib]
crate-type = ["cdylib"]
[dependencies]
libm = { version = "=0.2.16", default-features = false, features = ["force-soft-floats"] }
